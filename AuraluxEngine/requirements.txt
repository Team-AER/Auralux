# AuraluxEngine dependencies — local API server + ACE-Step MLX inference

# ML / Inference
mlx>=0.22.0
mlx-lm>=0.4.0
numpy>=1.26.0
safetensors>=0.4.0
transformers>=4.40.0
huggingface-hub>=0.23.0
soundfile>=0.12.1
librosa>=0.10.2
scipy>=1.13.0
torch>=2.3.0
torchaudio>=2.3.0
einops>=0.8.0
rotary-embedding-torch>=0.6.0

# Server
# (uses stdlib http.server — no extra deps needed)
